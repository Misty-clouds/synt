# dependencies
node_modules
.pnp
.pnp.js

# builds
dist
.next
out
build

# turbo
.turbo

# env — ignore real env files, but KEEP .env.example committed (reviewer reference)
.env
.env.local
.env.*.local
.env.prod
!.env.example
!**/.env.example

# design reference app dropped in at repo root (not part of the build)
/web/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# generated PDF case files (local storage fallback)
storage/
apps/api/storage/

# preview screenshots
.preview/

# misc
.DS_Store
*.pem
coverage
.vercel
*.tsbuildinfo

# traefik certificate store (contains private keys)
infra/traefik/acme.json
